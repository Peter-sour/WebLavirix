import React, { useState, useEffect, useRef } from 'react';
import { 
  Menu, X, CheckCircle, ArrowRight, Star, Shield, Battery, Wifi, Gift, 
  ChevronDown, ChevronUp, ShoppingBag, Truck, MessageCircle, Heart, 
  Share2, Zap, Smartphone, Activity, Watch, Search, CreditCard, User, 
  MapPin, Mail, Phone, Trash2, Plus, Minus, Lock, AlertCircle
} from 'lucide-react';

// --- DATA & KONFIGURASI MOCKUP ---

const PRODUCTS = {
  base: {
    id: 'chronos-base',
    name: 'Chronos Elite Series 5',
    price: 899000,
    originalPrice: 1800000,
    rating: 4.9,
    reviews: 2104,
    desc: 'Smartwatch flagship dengan bodi Titanium Aerospace-grade dan layar AMOLED 1.4 inch.',
    colors: [
      { name: 'Midnight Black', code: '#1a1a1a', image: 'https://images.unsplash.com/photo-1579586337278-3befd40fd17a?auto=format&fit=crop&w=600&q=80' },
      { name: 'Nebula Silver', code: '#e2e8f0', image: 'https://images.unsplash.com/photo-1523275335684-37898b6baf30?auto=format&fit=crop&w=600&q=80' },
      { name: 'Rose Gold', code: '#fda4af', image: 'https://images.unsplash.com/photo-1517502474097-f9b30659dadb?auto=format&fit=crop&w=600&q=80' }
    ]
  },
  pro: {
    id: 'chronos-pro',
    name: 'Chronos Elite Pro Max',
    price: 1299000,
    originalPrice: 2500000,
    rating: 5.0,
    reviews: 850,
    desc: 'Upgrade dengan Sapphire Glass, GPS Dual-Band, dan Strap Kulit Premium.',
    colors: [
      { name: 'Obsidian', code: '#000000', image: 'https://images.unsplash.com/photo-1544117519-31a4b719223d?auto=format&fit=crop&w=600&q=80' },
      { name: 'Deep Ocean', code: '#1e3a8a', image: 'https://images.unsplash.com/photo-1558525793-6e3e5c942918?auto=format&fit=crop&w=600&q=80' }
    ]
  }
};

const REVIEWS = [
  { id: 1, name: "Budi Santoso", role: "Atlet Lari", rating: 5, text: "GPS-nya gila akurat banget! Saya pakai Garmin sebelumnya, tapi Chronos Elite ini value for moneynya dapet banget. Batere tahan 8 hari pemakaian berat.", date: "2 Hari lalu" },
  { id: 2, name: "Siska Kohl", role: "Content Creator", rating: 5, text: "Modelnya cantik banget yang Rose Gold. Cocok buat outfit casual maupun formal. Notifikasi Instagram dan WA masuk lancar jaya.", date: "5 Hari lalu" },
  { id: 3, name: "Dr. Tirta", role: "Dokter Umum", rating: 4, text: "Fitur SpO2 dan Heart Rate cukup presisi untuk monitoring harian. Build quality solid, strap nyaman tidak bikin iritasi kulit.", date: "1 Minggu lalu" },
  { id: 4, name: "Kevin Anggara", role: "Tech Reviewer", rating: 5, text: "Layar AMOLED-nya tajem parah. Di bawah matahari terik masih kelihatan jelas. UI-nya smooth 60fps, ga patah-patah.", date: "2 Minggu lalu" },
  { id: 5, name: "Rina Nose", role: "Artis", rating: 5, text: "Pengiriman cepet banget, pesen pagi sore sampe. Packaging mewah, cocok buat kado suami.", date: "3 Minggu lalu" }
];

const FAQS = [
  { q: "Apakah Chronos Elite tahan air?", a: "Ya, bersertifikasi IP68 dan 5ATM. Aman untuk berenang di kolam renang (air tawar) hingga kedalaman 50 meter, mandi, atau terkena hujan. Tidak disarankan untuk diving atau air laut." },
  { q: "Bisa connect ke iPhone dan Android?", a: "Sangat bisa. Menggunakan aplikasi Chronos Fit yang tersedia gratis di Apple App Store (iOS 11+) dan Google Play Store (Android 6.0+)." },
  { q: "Berapa lama garansinya?", a: "Kami memberikan Garansi Resmi 12 Bulan ganti unit baru untuk kerusakan pabrik (bukan kesalahan pengguna seperti jatuh/pecah layar)." },
  { q: "Bagaimana cara klaim garansi?", a: "Cukup hubungi CS WhatsApp kami dengan menyertakan video kendala dan nomor pesanan. Tim kami akan memandu proses retur tanpa ribet." },
  { q: "Apakah bisa ganti strap/tali jam?", a: "Bisa banget. Chronos Elite menggunakan sistem Quick Release pin ukuran universal 22mm. Anda bisa beli strap aftermarket di mana saja." },
  { q: "Berapa lama pengisian daya?", a: "Menggunakan Fast Magnetic Charging, dari 0% ke 100% hanya butuh waktu sekitar 90 menit." }
];

const SPECS = {
  display: [
    { label: "Tipe Layar", value: "AMOLED Capacitive Touchscreen" },
    { label: "Ukuran", value: "1.43 Inch Round" },
    { label: "Resolusi", value: "466 x 466 pixels (~326 ppi)" },
    { label: "Pelindung", value: "Gorilla Glass DX+" },
    { label: "Always On Display", value: "Support" }
  ],
  battery: [
    { label: "Kapasitas", value: "480 mAh" },
    { label: "Daya Tahan", value: "Up to 14 Days (Basic), 7 Days (Heavy)" },
    { label: "Charging", value: "Magnetic Fast Charge" },
    { label: "Waktu Cas", value: "~1.5 Jam" }
  ],
  connectivity: [
    { label: "Bluetooth", value: "v5.3 LE (Low Energy)" },
    { label: "GPS", value: "Built-in GPS/GLONASS/Galileo" },
    { label: "NFC", value: "Yes (Access Card Only)" },
    { label: "Kompatibilitas", value: "Android 6.0+ / iOS 11.0+" }
  ],
  sensors: [
    { label: "Health", value: "Heart Rate, SpO2, Sleep Monitor, Stress" },
    { label: "Motion", value: "Accelerometer, Gyroscope, Compass" },
    { label: "Other", value: "Barometer, Ambient Light Sensor" }
  ]
};

const SALES_NOTIFICATIONS = [
  "Budi dari Jakarta baru saja membeli Chronos Elite Black",
  "Siti dari Surabaya baru saja membeli Chronos Elite Rose Gold",
  "Ahmad dari Bandung baru saja membeli Paket Couple",
  "Dewi dari Medan baru saja membeli Chronos Elite Silver",
  "Eko dari Semarang baru saja checkout 2 unit"
];

// --- SUB-COMPONENTS ---

const Badge = ({ children, color = "blue", className = "" }) => {
  const colors = {
    blue: "bg-blue-100 text-blue-700",
    red: "bg-red-100 text-red-700",
    green: "bg-green-100 text-green-700",
    purple: "bg-purple-100 text-purple-700",
    yellow: "bg-yellow-100 text-yellow-800"
  };
  return (
    <span className={`inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium ${colors[color]} ${className}`}>
      {children}
    </span>
  );
};

const SectionHeading = ({ sub, title, desc, align = "center" }) => (
  <div className={`mb-12 ${align === "center" ? "text-center" : "text-left"} max-w-4xl mx-auto px-4`}>
    {sub && <span className="text-blue-600 font-bold tracking-wider uppercase text-sm mb-2 block">{sub}</span>}
    <h2 className="text-3xl md:text-5xl font-extrabold text-gray-900 leading-tight mb-4">{title}</h2>
    {desc && <p className="text-lg text-gray-500 leading-relaxed">{desc}</p>}
  </div>
);

// --- MAIN APPLICATION ---

const App = () => {
  // State Management
  const [isMenuOpen, setIsMenuOpen] = useState(false);
  const [scrolled, setScrolled] = useState(false);
  const [scrollProgress, setScrollProgress] = useState(0);
  
  // Cart & Product State
  const [cart, setCart] = useState([]);
  const [isCartOpen, setIsCartOpen] = useState(false);
  const [isCheckoutOpen, setIsCheckoutOpen] = useState(false);
  const [selectedVariant, setSelectedVariant] = useState(0); // 0, 1, 2
  const [activeTab, setActiveTab] = useState('display');
  const [faqSearch, setFaqSearch] = useState('');
  
  // Notification State
  const [notification, setNotification] = useState(null);
  const [showNotification, setShowNotification] = useState(false);

  // Refs
  const heroRef = useRef(null);

  // --- EFFECTS ---

  // Scroll Listener
  useEffect(() => {
    const handleScroll = () => {
      const totalScroll = document.documentElement.scrollTop;
      const windowHeight = document.documentElement.scrollHeight - document.documentElement.clientHeight;
      const scroll = `${totalScroll / windowHeight}`;
      setScrollProgress(Number(scroll));
      setScrolled(window.scrollY > 20);
    };
    window.addEventListener('scroll', handleScroll);
    return () => window.removeEventListener('scroll', handleScroll);
  }, []);

  // Live Sales Notification Logic
  useEffect(() => {
    const interval = setInterval(() => {
      const randomMsg = SALES_NOTIFICATIONS[Math.floor(Math.random() * SALES_NOTIFICATIONS.length)];
      setNotification(randomMsg);
      setShowNotification(true);
      
      // Hide after 5 seconds
      setTimeout(() => {
        setShowNotification(false);
      }, 5000);
    }, 15000); // Show every 15 seconds

    return () => clearInterval(interval);
  }, []);

  // --- ACTIONS ---

  const addToCart = (product, variantIndex) => {
    const variant = product.colors[variantIndex];
    const newItem = {
      uid: Date.now(), // unique ID for cart item
      id: product.id,
      name: product.name,
      price: product.price,
      image: variant.image,
      color: variant.name
    };
    setCart([...cart, newItem]);
    setIsCartOpen(true);
  };

  const removeFromCart = (uid) => {
    setCart(cart.filter(item => item.uid !== uid));
  };

  const cartTotal = cart.reduce((acc, item) => acc + item.price, 0);

  const formatRupiah = (number) => {
    return new Intl.NumberFormat('id-ID', { style: 'currency', currency: 'IDR', minimumFractionDigits: 0 }).format(number);
  };

  const scrollTo = (id) => {
    const el = document.getElementById(id);
    if (el) {
      el.scrollIntoView({ behavior: 'smooth' });
      setIsMenuOpen(false);
    }
  };

  const filteredFAQs = FAQS.filter(f => 
    f.q.toLowerCase().includes(faqSearch.toLowerCase()) || 
    f.a.toLowerCase().includes(faqSearch.toLowerCase())
  );

  return (
    <div className="font-sans text-gray-800 bg-gray-50 min-h-screen selection:bg-blue-200 selection:text-blue-900 overflow-x-hidden">
      
      {/* SCROLL PROGRESS BAR */}
      <div className="fixed top-0 left-0 h-1 bg-gradient-to-r from-blue-500 to-purple-600 z-[60]" style={{ width: `${scrollProgress * 100}%` }}></div>

      {/* SALES NOTIFICATION TOAST */}
      <div className={`fixed bottom-24 left-4 md:bottom-8 md:left-8 z-40 bg-white p-4 rounded-xl shadow-2xl border-l-4 border-green-500 max-w-xs transform transition-all duration-500 ease-in-out ${showNotification ? 'translate-y-0 opacity-100' : 'translate-y-20 opacity-0 pointer-events-none'}`}>
        <div className="flex items-start gap-3">
          <div className="bg-green-100 p-2 rounded-full">
            <ShoppingBag size={16} className="text-green-600" />
          </div>
          <div>
            <p className="text-xs font-bold text-gray-500 uppercase">Baru saja terjual</p>
            <p className="text-sm font-medium text-gray-800 mt-1">{notification}</p>
            <p className="text-xs text-gray-400 mt-1">1 menit yang lalu</p>
          </div>
        </div>
      </div>

      {/* NAVBAR */}
      <nav className={`fixed w-full z-50 transition-all duration-300 ${scrolled ? 'bg-white/90 backdrop-blur-md shadow-lg py-3' : 'bg-transparent py-6'}`}>
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div className="flex justify-between items-center">
            {/* Logo */}
            <div className="flex items-center gap-2 cursor-pointer" onClick={() => window.scrollTo(0,0)}>
              <div className="bg-gradient-to-br from-blue-600 to-indigo-700 text-white p-2 rounded-xl shadow-lg">
                <Watch size={24} />
              </div>
              <div className="flex flex-col">
                <span className="text-xl md:text-2xl font-black text-gray-900 leading-none tracking-tight">CHRONOS</span>
                <span className="text-xs font-bold text-blue-600 tracking-widest uppercase">Elite Series</span>
              </div>
            </div>
            
            {/* Desktop Nav */}
            <div className="hidden lg:flex space-x-8 items-center">
              {['Fitur', 'Spesifikasi', 'Review', 'FAQ'].map((item) => (
                <button key={item} onClick={() => scrollTo(item.toLowerCase())} className="text-sm font-semibold text-gray-600 hover:text-blue-600 hover:bg-blue-50 px-3 py-2 rounded-lg transition-all">
                  {item}
                </button>
              ))}
            </div>

            {/* Actions */}
            <div className="flex items-center gap-4">
              <button 
                onClick={() => setIsCartOpen(true)}
                className="relative p-2 text-gray-600 hover:text-blue-600 hover:bg-gray-100 rounded-full transition-all"
              >
                <ShoppingBag size={24} />
                {cart.length > 0 && (
                  <span className="absolute top-0 right-0 h-5 w-5 bg-red-500 text-white text-[10px] font-bold flex items-center justify-center rounded-full border-2 border-white animate-bounce">
                    {cart.length}
                  </span>
                )}
              </button>
              
              <button onClick={() => scrollTo('pricing')} className="hidden md:flex bg-gray-900 text-white px-6 py-2.5 rounded-full font-bold text-sm hover:bg-gray-800 transition shadow-lg hover:shadow-gray-500/30 items-center gap-2">
                Beli Sekarang <ArrowRight size={16} />
              </button>

              <button onClick={() => setIsMenuOpen(!isMenuOpen)} className="lg:hidden text-gray-600">
                {isMenuOpen ? <X size={28} /> : <Menu size={28} />}
              </button>
            </div>
          </div>
        </div>

        {/* Mobile Menu */}
        <div className={`lg:hidden absolute top-full left-0 w-full bg-white shadow-xl border-t border-gray-100 transition-all duration-300 overflow-hidden ${isMenuOpen ? 'max-h-96 opacity-100' : 'max-h-0 opacity-0'}`}>
          <div className="p-4 space-y-2 flex flex-col">
            {['Fitur', 'Spesifikasi', 'Review', 'FAQ'].map((item) => (
              <button key={item} onClick={() => scrollTo(item.toLowerCase())} className="text-left px-4 py-3 font-semibold text-gray-700 hover:bg-gray-50 rounded-xl">
                {item}
              </button>
            ))}
            <button onClick={() => scrollTo('pricing')} className="mt-2 w-full bg-blue-600 text-white py-3 rounded-xl font-bold">
              Lihat Harga Promo
            </button>
          </div>
        </div>
      </nav>

      {/* HERO SECTION */}
      {/* Mengubah padding-top dari pt-32/md:pt-48 menjadi pt-24/md:pt-32 agar konten lebih naik ke atas */}
      <section className="relative pt-24 pb-16 md:pt-32 md:pb-24 overflow-hidden bg-[#F8FAFC]">
        {/* Abstract Background Blobs */}
        <div className="absolute top-0 left-0 w-full h-full overflow-hidden pointer-events-none">
          <div className="absolute -top-[20%] -right-[10%] w-[700px] h-[700px] bg-blue-300/20 rounded-full blur-3xl animate-pulse"></div>
          <div className="absolute top-[40%] -left-[10%] w-[500px] h-[500px] bg-purple-300/20 rounded-full blur-3xl animate-pulse" style={{ animationDelay: '2s' }}></div>
        </div>

        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 relative z-10">
          <div className="grid lg:grid-cols-2 gap-12 lg:gap-20 items-center">
            
            {/* Left Content */}
            <div className="space-y-8 text-center lg:text-left">
              <div className="inline-flex items-center gap-2 px-4 py-2 rounded-full bg-white border border-blue-100 shadow-sm text-blue-700 text-sm font-bold animate-fade-in-up">
                <span className="relative flex h-3 w-3">
                  <span className="animate-ping absolute inline-flex h-full w-full rounded-full bg-blue-400 opacity-75"></span>
                  <span className="relative inline-flex rounded-full h-3 w-3 bg-blue-500"></span>
                </span>
                Produk Terlaris 2024
              </div>
              
              <h1 className="text-5xl lg:text-7xl font-black text-gray-900 leading-[1.1] tracking-tight">
                Masa Depan <br/>
                <span className="bg-clip-text text-transparent bg-gradient-to-r from-blue-600 via-indigo-600 to-purple-600">
                  Di Pergelanganmu.
                </span>
              </h1>
              
              <p className="text-xl text-gray-600 leading-relaxed max-w-lg mx-auto lg:mx-0">
                Teknologi kesehatan tercanggih, desain titanium premium, dan baterai 14 hari. Chronos Elite bukan sekadar jam, ini adalah asisten hidup Anda.
              </p>

              <div className="flex flex-col sm:flex-row gap-4 justify-center lg:justify-start pt-4">
                <button onClick={() => scrollTo('pricing')} className="group bg-blue-600 text-white px-8 py-4 rounded-2xl font-bold text-lg hover:bg-blue-700 transition shadow-xl hover:shadow-blue-500/40 flex items-center justify-center gap-3">
                  Pesan Sekarang 
                  <ArrowRight className="group-hover:translate-x-1 transition-transform" />
                </button>
                <button onClick={() => scrollTo('video')} className="group bg-white text-gray-800 border-2 border-gray-200 px-8 py-4 rounded-2xl font-bold text-lg hover:border-gray-800 hover:text-gray-900 transition flex items-center justify-center gap-3">
                  <div className="w-8 h-8 rounded-full bg-gray-100 flex items-center justify-center group-hover:bg-gray-200">
                    <div className="w-0 h-0 border-t-[6px] border-t-transparent border-l-[10px] border-l-gray-800 border-b-[6px] border-b-transparent ml-1"></div>
                  </div>
                  Tonton Video
                </button>
              </div>

              <div className="pt-8 flex items-center justify-center lg:justify-start gap-8 border-t border-gray-200/60 mt-8">
                <div>
                  <p className="text-3xl font-bold text-gray-900">50K+</p>
                  <p className="text-sm text-gray-500 font-medium">Pengguna Aktif</p>
                </div>
                <div className="w-px h-10 bg-gray-300"></div>
                <div>
                  <p className="text-3xl font-bold text-gray-900">4.9</p>
                  <p className="text-sm text-gray-500 font-medium">Rating Rata-rata</p>
                </div>
                <div className="w-px h-10 bg-gray-300"></div>
                <div>
                  <p className="text-3xl font-bold text-gray-900">1th</p>
                  <p className="text-sm text-gray-500 font-medium">Garansi Resmi</p>
                </div>
              </div>
            </div>

            {/* Right Content (Product Visualizer) */}
            <div className="relative">
               <div className="absolute inset-0 bg-gradient-to-tr from-blue-100 to-purple-100 rounded-[3rem] transform rotate-6 scale-95 z-0"></div>
               <div className="relative z-10 bg-white/40 backdrop-blur-xl border border-white/50 p-8 rounded-[2.5rem] shadow-2xl">
                 
                 {/* Main Image */}
                 <div className="relative aspect-square rounded-2xl overflow-hidden bg-gray-100 mb-6 group">
                   <img 
                      src={PRODUCTS.base.colors[selectedVariant].image} 
                      alt="Chronos Watch" 
                      className="w-full h-full object-cover transform transition-transform duration-700 group-hover:scale-110"
                   />
                   
                   {/* Floating Tags */}
                   <div className="absolute top-4 left-4 bg-black/80 backdrop-blur text-white px-3 py-1 rounded-lg text-xs font-bold">
                     New Arrival
                   </div>
                   <div className="absolute bottom-4 right-4 bg-white/90 backdrop-blur text-gray-900 px-4 py-2 rounded-xl text-sm font-bold shadow-lg flex items-center gap-2">
                     <Heart className="text-red-500 fill-current" size={16} />
                     Most Loved
                   </div>
                 </div>

                 {/* Color Selector */}
                 <div className="bg-white p-4 rounded-xl shadow-sm border border-gray-100 flex justify-between items-center">
                   <div>
                     <p className="text-xs text-gray-500 font-bold uppercase tracking-wider mb-2">Pilih Warna</p>
                     <div className="flex gap-3">
                       {PRODUCTS.base.colors.map((color, idx) => (
                         <button
                            key={idx}
                            onClick={() => setSelectedVariant(idx)}
                            className={`w-8 h-8 rounded-full border-2 transition-all ${selectedVariant === idx ? 'border-blue-600 scale-110 shadow-md' : 'border-transparent hover:scale-110'}`}
                            style={{ backgroundColor: color.code }}
                            title={color.name}
                         >
                           {selectedVariant === idx && <CheckCircle size={12} className="text-white mx-auto drop-shadow-md" />}
                         </button>
                       ))}
                     </div>
                   </div>
                   <div className="text-right">
                     <p className="text-xs text-gray-500 mb-1 font-medium">{PRODUCTS.base.colors[selectedVariant].name}</p>
                     <p className="text-xl font-bold text-gray-900">{formatRupiah(PRODUCTS.base.price)}</p>
                   </div>
                 </div>

                 {/* Quick Add to Cart */}
                 <button 
                  onClick={() => addToCart(PRODUCTS.base, selectedVariant)}
                  className="w-full mt-4 bg-gray-900 text-white py-4 rounded-xl font-bold hover:bg-gray-800 transition flex justify-center items-center gap-2"
                 >
                   <ShoppingBag size={18} /> Masukkan Keranjang
                 </button>
               </div>
            </div>

          </div>
        </div>
      </section>

      {/* BRAND MARQUEE */}
      <div className="bg-white border-y border-gray-100 py-8 overflow-hidden">
        <p className="text-center text-sm font-bold text-gray-400 uppercase tracking-[0.2em] mb-6">Diliput & Dipercaya Oleh</p>
        <div className="flex justify-center gap-8 md:gap-16 opacity-40 grayscale items-center flex-wrap px-4">
           {/* Simple Text Logos for demo */}
           <h3 className="text-2xl font-black font-serif">FORBES</h3>
           <h3 className="text-2xl font-black tracking-tighter">TECHCRUNCH</h3>
           <h3 className="text-2xl font-bold italic">The Verge</h3>
           <h3 className="text-2xl font-bold">WIRED</h3>
           <h3 className="text-2xl font-black font-mono">CNBC</h3>
        </div>
      </div>

      {/* PROBLEM & SOLUTION SECTION */}
      <section className="py-24 bg-white">
        <div className="max-w-4xl mx-auto px-4 text-center">
          <Badge color="red" className="mb-6">Masalah Umum Smartwatch</Badge>
          <h2 className="text-3xl md:text-5xl font-bold text-gray-900 mb-8 leading-tight">
            "Baterai Habis Saat Lari?" <br/>
            "Layar Buram Saat Siang?" <br/>
            "Strap Bikin Gatal?"
          </h2>
          <p className="text-xl text-gray-600 mb-12">
            Kami mendengar keluhan Anda. Kebanyakan smartwatch di pasar mengorbankan kualitas demi harga murah, atau terlalu mahal hanya untuk nama brand.
          </p>
        </div>
        
        <div className="max-w-6xl mx-auto px-4 grid md:grid-cols-3 gap-8">
           <div className="bg-gray-50 p-8 rounded-3xl border border-gray-100 relative overflow-hidden group">
             <div className="absolute top-0 right-0 p-8 opacity-10 group-hover:opacity-20 transition-opacity">
               <Battery size={100} />
             </div>
             <h3 className="text-xl font-bold mb-4">Solusi Baterai Lemah</h3>
             <p className="text-gray-600">Chipset AI hemat daya kami membuat Chronos bertahan hingga 14 hari tanpa charger.</p>
           </div>
           <div className="bg-gray-50 p-8 rounded-3xl border border-gray-100 relative overflow-hidden group">
             <div className="absolute top-0 right-0 p-8 opacity-10 group-hover:opacity-20 transition-opacity">
               <Smartphone size={100} />
             </div>
             <h3 className="text-xl font-bold mb-4">Solusi Koneksi Putus</h3>
             <p className="text-gray-600">Antena Bluetooth keramik khusus menjamin koneksi stabil hingga radius 20 meter.</p>
           </div>
           <div className="bg-gray-50 p-8 rounded-3xl border border-gray-100 relative overflow-hidden group">
             <div className="absolute top-0 right-0 p-8 opacity-10 group-hover:opacity-20 transition-opacity">
               <Activity size={100} />
             </div>
             <h3 className="text-xl font-bold mb-4">Solusi Sensor Ngaco</h3>
             <p className="text-gray-600">Dual-sensor optik generasi ke-4 memberikan akurasi setara alat medis RS.</p>
           </div>
        </div>
      </section>

      {/* FEATURE GRID */}
      <section id="fitur" className="py-24 bg-gray-900 text-white">
        <div className="max-w-7xl mx-auto px-4">
          <SectionHeading 
            sub="Fitur Unggulan" 
            title="Diciptakan Untuk Performa" 
            desc="Setiap inci dari Chronos Elite dirancang dengan presisi untuk mendukung gaya hidup aktif Anda."
            titleClass="text-white"
          />
          <div className="mt-8 mb-12 grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
            {[
              { icon: <Heart className="text-red-500" size={32} />, title: "Health Monitor 24/7", desc: "Detak jantung, stress, & tidur." },
              { icon: <Zap className="text-yellow-500" size={32} />, title: "100+ Mode Olahraga", desc: "Dari lari, renang, hingga yoga." },
              { icon: <MessageCircle className="text-green-500" size={32} />, title: "Smart Notification", desc: "Baca WA & tolak telepon." },
              { icon: <MapPin className="text-blue-500" size={32} />, title: "Built-in GPS", desc: "Lacak rute tanpa bawa HP." },
            ].map((feature, i) => (
              <div key={i} className="bg-gray-800/50 p-6 rounded-2xl border border-gray-700 hover:bg-gray-800 transition duration-300">
                <div className="mb-4 bg-gray-900 w-14 h-14 rounded-xl flex items-center justify-center border border-gray-700">
                  {feature.icon}
                </div>
                <h3 className="text-lg font-bold mb-2 text-white">{feature.title}</h3>
                <p className="text-gray-400 text-sm">{feature.desc}</p>
              </div>
            ))}
          </div>

          {/* Large Visual Feature */}
          <div className="grid lg:grid-cols-2 gap-12 items-center bg-gray-800 rounded-[3rem] p-8 lg:p-12 border border-gray-700">
            <div className="order-2 lg:order-1 space-y-6">
              <Badge color="blue">Teknologi Layar Terbaru</Badge>
              <h3 className="text-3xl lg:text-4xl font-bold">Retina AMOLED Display. <br/>Terang. Tajam. Luas.</h3>
              <p className="text-gray-300 text-lg">
                Dengan kecerahan hingga 1000 nits, layar Chronos Elite tetap terlihat jelas meski di bawah matahari pantai yang terik. Bezel tipis memberikan rasio layar-ke-bodi yang imersif.
              </p>
              <ul className="space-y-4 pt-4">
                {[
                  "Resolusi 466x466 pixel yang ultra tajam",
                  "Fitur Always-On Display hemat daya",
                  "Ribuan Watch Face gratis bisa diganti-ganti"
                ].map((item, idx) => (
                  <li key={idx} className="flex items-center gap-3 text-gray-300">
                    <CheckCircle className="text-blue-500 flex-shrink-0" size={20} />
                    {item}
                  </li>
                ))}
              </ul>
            </div>
            <div className="order-1 lg:order-2 relative">
              <div className="absolute inset-0 bg-blue-500/20 blur-3xl rounded-full"></div>
              <img 
                src="https://images.unsplash.com/photo-1551816230-ef5deaed4a26?auto=format&fit=crop&w=800&q=80" 
                alt="Watch Screen" 
                className="relative z-10 rounded-2xl shadow-2xl border-4 border-gray-700 transform rotate-2 hover:rotate-0 transition duration-500"
              />
            </div>
          </div>
        </div>
      </section>

      {/* TECH SPECS TABS */}
      <section id="spesifikasi" className="py-24 bg-white">
        <div className="max-w-5xl mx-auto px-4">
          <SectionHeading 
            title="Spesifikasi Teknis" 
            desc="Transparansi total. Inilah jeroan mesin yang membuat Chronos Elite begitu bertenaga."
          />
          
          <div className="flex flex-wrap justify-center gap-2 mb-12">
            {Object.keys(SPECS).map((key) => (
              <button
                key={key}
                onClick={() => setActiveTab(key)}
                className={`px-6 py-3 rounded-full font-bold text-sm uppercase tracking-wide transition-all ${activeTab === key ? 'bg-blue-600 text-white shadow-lg scale-105' : 'bg-gray-100 text-gray-500 hover:bg-gray-200'}`}
              >
                {key}
              </button>
            ))}
          </div>

          <div className="bg-white border border-gray-200 rounded-3xl p-8 shadow-xl min-h-[300px]">
            <div className="grid md:grid-cols-2 gap-y-4 gap-x-12">
              {SPECS[activeTab].map((item, idx) => (
                <div key={idx} className="flex justify-between items-center py-4 border-b border-gray-100 hover:bg-gray-50 px-4 rounded-lg transition">
                  <span className="text-gray-500 font-medium">{item.label}</span>
                  <span className="text-gray-900 font-bold text-right">{item.value}</span>
                </div>
              ))}
            </div>
          </div>
        </div>
      </section>

      {/* GALLERY GRID */}
      <section className="py-12">
        <div className="grid grid-cols-2 md:grid-cols-4 gap-2 md:gap-4 px-4">
          {[
            "https://images.unsplash.com/photo-1434493789847-2f02ea6ca920?auto=format&fit=crop&w=600&q=80",
            "https://images.unsplash.com/photo-1508685096489-7aacd43bd3b1?auto=format&fit=crop&w=600&q=80",
            "https://images.unsplash.com/photo-1542496658-e33a6d0d50f6?auto=format&fit=crop&w=600&q=80",
            "https://images.unsplash.com/photo-1510017803434-a899398421b3?auto=format&fit=crop&w=600&q=80"
          ].map((src, i) => (
            <div key={i} className={`relative overflow-hidden group rounded-2xl ${i === 0 ? 'col-span-2 row-span-2' : ''} h-full min-h-[200px]`}>
              <img src={src} alt="Gallery" className="w-full h-full object-cover transition duration-700 group-hover:scale-110" />
              <div className="absolute inset-0 bg-black/20 group-hover:bg-black/40 transition"></div>
            </div>
          ))}
        </div>
      </section>

      {/* REVIEWS & TESTIMONIALS */}
      <section id="review" className="py-24 bg-blue-50">
        <div className="max-w-7xl mx-auto px-4">
          <SectionHeading 
            sub="Social Proof" 
            title="Ribuan Pengguna Puas" 
            desc="Jangan hanya percaya kata kami. Dengar apa kata mereka yang sudah menggunakan Chronos Elite setiap hari."
          />

          <div className="grid md:grid-cols-3 gap-8">
             {REVIEWS.slice(0, 3).map((review) => (
               <div key={review.id} className="bg-white p-8 rounded-3xl shadow-sm border border-gray-100 flex flex-col h-full hover:shadow-xl hover:-translate-y-2 transition duration-300">
                 <div className="flex gap-1 mb-6">
                   {[...Array(5)].map((_, i) => (
                     <Star key={i} size={18} className={i < review.rating ? "text-yellow-400 fill-current" : "text-gray-300"} />
                   ))}
                 </div>
                 <p className="text-gray-700 mb-6 italic flex-grow leading-relaxed">"{review.text}"</p>
                 <div className="flex items-center gap-4 mt-auto border-t border-gray-100 pt-6">
                   <div className="w-12 h-12 bg-gradient-to-br from-blue-100 to-indigo-100 rounded-full flex items-center justify-center text-blue-700 font-bold text-lg">
                     {review.name.charAt(0)}
                   </div>
                   <div>
                     <h4 className="font-bold text-gray-900">{review.name}</h4>
                     <p className="text-xs text-gray-500">{review.role} • {review.date}</p>
                   </div>
                 </div>
               </div>
             ))}
          </div>

          <div className="mt-12 text-center">
            <button className="text-blue-600 font-bold hover:text-blue-800 flex items-center justify-center gap-2 mx-auto">
              Lihat 2,100+ Review Lainnya <ArrowRight size={16}/>
            </button>
          </div>
        </div>
      </section>

      {/* PRICING TABLE */}
      <section id="pricing" className="py-24 bg-white relative overflow-hidden">
        <div className="absolute top-0 left-1/2 -translate-x-1/2 w-[1000px] h-[500px] bg-blue-50 rounded-full blur-3xl opacity-50 z-0"></div>
        <div className="max-w-7xl mx-auto px-4 relative z-10">
          <SectionHeading 
            title="Investasi Kesehatan Anda" 
            desc="Harga promo spesial hanya berlaku untuk batch produksi bulan ini."
          />

          <div className="grid lg:grid-cols-2 gap-8 max-w-4xl mx-auto items-start">
            
            {/* Base Model */}
            <div className="bg-white rounded-3xl p-8 border border-gray-200 shadow-lg hover:shadow-2xl transition duration-300 relative">
              <h3 className="text-2xl font-bold text-gray-900">{PRODUCTS.base.name}</h3>
              <p className="text-gray-500 mt-2 text-sm">{PRODUCTS.base.desc}</p>
              
              <div className="mt-6 mb-8">
                <span className="text-gray-400 line-through text-lg">{formatRupiah(PRODUCTS.base.originalPrice)}</span>
                <div className="flex items-baseline gap-2">
                   <span className="text-4xl font-black text-gray-900">{formatRupiah(PRODUCTS.base.price)}</span>
                </div>
              </div>

              <ul className="space-y-4 mb-8">
                <li className="flex items-center gap-3 text-sm text-gray-700"><CheckCircle size={18} className="text-green-500"/> Layar AMOLED 1.4"</li>
                <li className="flex items-center gap-3 text-sm text-gray-700"><CheckCircle size={18} className="text-green-500"/> Baterai 7 Hari</li>
                <li className="flex items-center gap-3 text-sm text-gray-700"><CheckCircle size={18} className="text-green-500"/> Strap Silikon Sport</li>
                <li className="flex items-center gap-3 text-sm text-gray-700"><CheckCircle size={18} className="text-green-500"/> Garansi 12 Bulan</li>
              </ul>

              <button 
                onClick={() => {
                   setSelectedVariant(0);
                   addToCart(PRODUCTS.base, 0);
                }}
                className="w-full py-4 rounded-xl border-2 border-gray-900 font-bold hover:bg-gray-900 hover:text-white transition"
              >
                Pilih Paket Basic
              </button>
            </div>

            {/* Pro Model */}
            <div className="bg-gray-900 rounded-3xl p-8 border border-gray-800 shadow-2xl text-white relative transform md:-translate-y-4">
              <div className="absolute top-0 right-0 bg-gradient-to-r from-blue-600 to-purple-600 text-white text-xs font-bold px-4 py-1.5 rounded-bl-xl rounded-tr-2xl uppercase tracking-wider">
                Best Seller
              </div>
              
              <h3 className="text-2xl font-bold text-white">{PRODUCTS.pro.name}</h3>
              <p className="text-gray-400 mt-2 text-sm">{PRODUCTS.pro.desc}</p>
              
              <div className="mt-6 mb-8">
                <span className="text-gray-600 line-through text-lg">{formatRupiah(PRODUCTS.pro.originalPrice)}</span>
                <div className="flex items-baseline gap-2">
                   <span className="text-4xl font-black text-white">{formatRupiah(PRODUCTS.pro.price)}</span>
                </div>
                <p className="text-green-400 text-xs font-bold mt-1">HEMAT {formatRupiah(PRODUCTS.pro.originalPrice - PRODUCTS.pro.price)}</p>
              </div>

              <div className="space-y-4 mb-8">
                <p className="text-sm font-bold text-gray-400 uppercase tracking-widest">Semua fitur Basic, ditambah:</p>
                <ul className="space-y-4">
                  <li className="flex items-center gap-3 text-sm text-gray-200"><CheckCircle size={18} className="text-blue-500"/> <strong>Sapphire Glass</strong> (Anti Gores)</li>
                  <li className="flex items-center gap-3 text-sm text-gray-200"><CheckCircle size={18} className="text-blue-500"/> <strong>GPS Dual-Band</strong> (Lebih Akurat)</li>
                  <li className="flex items-center gap-3 text-sm text-gray-200"><CheckCircle size={18} className="text-blue-500"/> <strong>Bonus Strap Kulit</strong> Premium</li>
                  <li className="flex items-center gap-3 text-sm text-gray-200"><CheckCircle size={18} className="text-blue-500"/> Prioritas Garansi (Tukar Baru)</li>
                </ul>
              </div>

              <button 
                onClick={() => {
                   // Add Pro logic specifically
                   addToCart({
                     ...PRODUCTS.pro,
                     colors: PRODUCTS.pro.colors // Pass colors for pro
                   }, 0);
                }}
                className="w-full py-4 rounded-xl bg-gradient-to-r from-blue-600 to-indigo-600 font-bold text-white shadow-lg hover:shadow-blue-500/50 hover:scale-[1.02] transition-all"
              >
                Ambil Paket Pro Max
              </button>
              <p className="text-center text-xs text-gray-500 mt-4">Gratis Ongkir Seluruh Indonesia</p>
            </div>

          </div>
        </div>
      </section>

      {/* FAQ SEARCHABLE */}
      <section id="faq" className="py-24 bg-gray-50">
        <div className="max-w-3xl mx-auto px-4">
          <SectionHeading title="Pertanyaan Umum" />
          
          <div className="relative mb-8">
            <input 
              type="text" 
              placeholder="Cari pertanyaan Anda..." 
              value={faqSearch}
              onChange={(e) => setFaqSearch(e.target.value)}
              className="w-full px-6 py-4 rounded-xl border border-gray-200 focus:outline-none focus:ring-2 focus:ring-blue-500 shadow-sm pl-12"
            />
            <Search className="absolute left-4 top-1/2 -translate-y-1/2 text-gray-400" size={20} />
          </div>

          <div className="space-y-4">
            {filteredFAQs.length > 0 ? filteredFAQs.map((faq, i) => (
              <details key={i} className="group bg-white rounded-xl border border-gray-200 overflow-hidden cursor-pointer transition-all duration-300 open:shadow-md">
                <summary className="flex justify-between items-center p-6 font-semibold text-gray-900 list-none select-none">
                  {faq.q}
                  <span className="transition group-open:rotate-180">
                    <ChevronDown size={20} />
                  </span>
                </summary>
                <div className="p-6 pt-0 text-gray-600 leading-relaxed border-t border-transparent group-open:border-gray-100">
                  {faq.a}
                </div>
              </details>
            )) : (
              <p className="text-center text-gray-500 py-8">Pertanyaan tidak ditemukan. Hubungi CS kami.</p>
            )}
          </div>
        </div>
      </section>

      {/* FINAL CTA */}
      <section className="py-24 bg-black relative overflow-hidden">
        <div className="absolute inset-0 bg-[url('https://images.unsplash.com/photo-1550751827-4bd374c3f58b?auto=format&fit=crop&w=1600&q=80')] bg-cover bg-center opacity-20"></div>
        <div className="absolute inset-0 bg-gradient-to-t from-black via-black/80 to-transparent"></div>
        
        <div className="relative z-10 max-w-4xl mx-auto px-4 text-center">
          <h2 className="text-4xl md:text-6xl font-black text-white mb-8">Jangan Lewatkan <br/><span className="text-blue-500">Revolusi Ini.</span></h2>
          <p className="text-gray-400 text-xl mb-10 max-w-2xl mx-auto">
            Stok batch ini tersisa kurang dari 50 unit. Amankan milik Anda sekarang sebelum harga naik kembali normal.
          </p>
          <div className="flex flex-col sm:flex-row gap-4 justify-center">
            <button 
              onClick={() => scrollTo('pricing')} 
              className="bg-white text-black px-10 py-5 rounded-full font-bold text-lg hover:bg-gray-200 transition transform hover:-translate-y-1"
            >
              Pesan Sekarang
            </button>
            <button className="bg-transparent border border-gray-600 text-white px-10 py-5 rounded-full font-bold text-lg hover:bg-gray-900 transition flex items-center justify-center gap-2">
              <MessageCircle size={20}/> Konsultasi WhatsApp
            </button>
          </div>
        </div>
      </section>

      {/* FOOTER */}
      <footer className="bg-gray-900 text-gray-300 pt-20 pb-10 border-t border-gray-800">
        <div className="max-w-7xl mx-auto px-4 grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-12 mb-16">
          <div className="space-y-6">
            <div className="flex items-center gap-2 text-white">
              <Watch size={24} />
              <span className="text-2xl font-black">CHRONOS</span>
            </div>
            <p className="text-sm leading-relaxed text-gray-400">
              Chronos Elite adalah merek dagang terdaftar. Kami berdedikasi menciptakan teknologi wearable yang dapat diakses oleh semua orang dengan kualitas premium.
            </p>
            <div className="flex gap-4">
              <div className="w-10 h-10 bg-gray-800 rounded-full flex items-center justify-center hover:bg-blue-600 hover:text-white transition cursor-pointer"><Share2 size={18}/></div>
              <div className="w-10 h-10 bg-gray-800 rounded-full flex items-center justify-center hover:bg-blue-600 hover:text-white transition cursor-pointer"><Mail size={18}/></div>
              <div className="w-10 h-10 bg-gray-800 rounded-full flex items-center justify-center hover:bg-blue-600 hover:text-white transition cursor-pointer"><Phone size={18}/></div>
            </div>
          </div>
          
          <div>
            <h4 className="text-white font-bold mb-6">Produk</h4>
            <ul className="space-y-4 text-sm">
              <li><a href="#" className="hover:text-blue-500 transition">Chronos Elite Series 5</a></li>
              <li><a href="#" className="hover:text-blue-500 transition">Chronos Pro Max</a></li>
              <li><a href="#" className="hover:text-blue-500 transition">Aksesoris Strap</a></li>
              <li><a href="#" className="hover:text-blue-500 transition">Charger & Cables</a></li>
            </ul>
          </div>

          <div>
            <h4 className="text-white font-bold mb-6">Dukungan</h4>
            <ul className="space-y-4 text-sm">
              <li><a href="#" className="hover:text-blue-500 transition">Cek Status Pesanan</a></li>
              <li><a href="#" className="hover:text-blue-500 transition">Kebijakan Garansi</a></li>
              <li><a href="#" className="hover:text-blue-500 transition">Pusat Bantuan</a></li>
              <li><a href="#" className="hover:text-blue-500 transition">Syarat & Ketentuan</a></li>
            </ul>
          </div>

          <div>
            <h4 className="text-white font-bold mb-6">Newsletter</h4>
            <p className="text-sm text-gray-400 mb-4">Dapatkan info promo dan rilis produk baru.</p>
            <div className="flex gap-2">
              <input type="email" placeholder="Email Anda" className="bg-gray-800 border border-gray-700 text-white text-sm rounded-lg px-4 py-3 w-full focus:outline-none focus:border-blue-500"/>
              <button className="bg-blue-600 text-white rounded-lg px-4 hover:bg-blue-700 transition">
                <ArrowRight size={18}/>
              </button>
            </div>
          </div>
        </div>

        <div className="max-w-7xl mx-auto px-4 border-t border-gray-800 pt-8 flex flex-col md:flex-row justify-between items-center gap-4">
          <p className="text-xs text-gray-500">&copy; 2024 Chronos Tech Indonesia. All rights reserved.</p>
          <div className="flex gap-4">
             {/* Payment Icons Mockup */}
             <div className="h-6 w-10 bg-gray-700 rounded opacity-50"></div>
             <div className="h-6 w-10 bg-gray-700 rounded opacity-50"></div>
             <div className="h-6 w-10 bg-gray-700 rounded opacity-50"></div>
             <div className="h-6 w-10 bg-gray-700 rounded opacity-50"></div>
          </div>
        </div>
      </footer>

      {/* --- MODALS & OVERLAYS --- */}

      {/* CART DRAWER */}
      {isCartOpen && (
        <div className="fixed inset-0 z-[60] flex justify-end">
          <div className="absolute inset-0 bg-black/50 backdrop-blur-sm" onClick={() => setIsCartOpen(false)}></div>
          <div className="relative bg-white w-full max-w-md h-full shadow-2xl flex flex-col transform transition-transform duration-300">
            <div className="p-6 border-b border-gray-100 flex justify-between items-center bg-gray-50">
              <h2 className="text-xl font-bold flex items-center gap-2"><ShoppingBag size={20}/> Keranjang ({cart.length})</h2>
              <button onClick={() => setIsCartOpen(false)} className="p-2 hover:bg-gray-200 rounded-full transition"><X size={20}/></button>
            </div>
            
            <div className="flex-1 overflow-y-auto p-6 space-y-6">
              {cart.length === 0 ? (
                <div className="h-full flex flex-col items-center justify-center text-center space-y-4 opacity-50">
                  <ShoppingBag size={64} className="text-gray-300"/>
                  <p className="text-lg font-medium">Keranjang masih kosong</p>
                  <button onClick={() => setIsCartOpen(false)} className="text-blue-600 font-bold hover:underline">Mulai Belanja</button>
                </div>
              ) : (
                cart.map((item) => (
                  <div key={item.uid} className="flex gap-4">
                    <div className="w-20 h-20 bg-gray-100 rounded-xl overflow-hidden flex-shrink-0 border border-gray-200">
                      <img src={item.image} alt={item.name} className="w-full h-full object-cover"/>
                    </div>
                    <div className="flex-1">
                      <h4 className="font-bold text-gray-900 line-clamp-1">{item.name}</h4>
                      <p className="text-sm text-gray-500 mb-2">Warna: {item.color}</p>
                      <div className="flex justify-between items-center">
                        <span className="font-bold text-blue-600">{formatRupiah(item.price)}</span>
                        <button onClick={() => removeFromCart(item.uid)} className="text-red-500 hover:bg-red-50 p-1.5 rounded-lg transition">
                          <Trash2 size={16}/>
                        </button>
                      </div>
                    </div>
                  </div>
                ))
              )}
            </div>

            {cart.length > 0 && (
              <div className="p-6 border-t border-gray-100 bg-gray-50 space-y-4">
                <div className="flex justify-between text-lg font-bold">
                  <span>Total</span>
                  <span>{formatRupiah(cartTotal)}</span>
                </div>
                <button 
                  onClick={() => {
                    setIsCartOpen(false);
                    setIsCheckoutOpen(true);
                  }}
                  className="w-full bg-blue-600 text-white py-4 rounded-xl font-bold hover:bg-blue-700 transition shadow-lg"
                >
                  Checkout Sekarang
                </button>
                <p className="text-xs text-center text-gray-500 flex items-center justify-center gap-1">
                  <Lock size={12}/> Transaksi 256-bit SSL Secured
                </p>
              </div>
            )}
          </div>
        </div>
      )}

      {/* CHECKOUT MODAL */}
      {isCheckoutOpen && (
        <div className="fixed inset-0 z-[70] flex items-center justify-center p-4">
          <div className="absolute inset-0 bg-black/60 backdrop-blur-sm" onClick={() => setIsCheckoutOpen(false)}></div>
          <div className="bg-white w-full max-w-2xl rounded-3xl shadow-2xl relative z-10 overflow-hidden flex flex-col max-h-[90vh]">
            <div className="p-6 border-b border-gray-100 flex justify-between items-center bg-gray-50">
              <h3 className="text-xl font-bold">Formulir Pemesanan</h3>
              <button onClick={() => setIsCheckoutOpen(false)}><X size={24} className="text-gray-400 hover:text-gray-600"/></button>
            </div>
            
            <div className="flex-1 overflow-y-auto p-6 md:p-8">
              <div className="space-y-6">
                {/* Summary */}
                <div className="bg-blue-50 p-4 rounded-xl border border-blue-100 flex justify-between items-center">
                  <div>
                    <p className="text-xs font-bold text-blue-600 uppercase tracking-wide">Ringkasan Order</p>
                    <p className="font-bold text-gray-900">{cart.length} Item di Keranjang</p>
                  </div>
                  <span className="text-xl font-black text-blue-700">{formatRupiah(cartTotal)}</span>
                </div>

                <form className="space-y-4" onSubmit={(e) => e.preventDefault()}>
                  <div className="grid md:grid-cols-2 gap-4">
                    <div>
                      <label className="block text-sm font-bold text-gray-700 mb-1">Nama Lengkap</label>
                      <div className="relative">
                        <User className="absolute left-3 top-1/2 -translate-y-1/2 text-gray-400" size={18}/>
                        <input type="text" className="w-full pl-10 pr-4 py-3 rounded-lg border border-gray-300 focus:ring-2 focus:ring-blue-500 focus:outline-none" placeholder="Contoh: Budi Santoso"/>
                      </div>
                    </div>
                    <div>
                      <label className="block text-sm font-bold text-gray-700 mb-1">Nomor WhatsApp</label>
                      <div className="relative">
                        <Phone className="absolute left-3 top-1/2 -translate-y-1/2 text-gray-400" size={18}/>
                        <input type="tel" className="w-full pl-10 pr-4 py-3 rounded-lg border border-gray-300 focus:ring-2 focus:ring-blue-500 focus:outline-none" placeholder="0812xxxx"/>
                      </div>
                    </div>
                  </div>

                  <div>
                    <label className="block text-sm font-bold text-gray-700 mb-1">Alamat Lengkap</label>
                    <div className="relative">
                      <MapPin className="absolute left-3 top-3 text-gray-400" size={18}/>
                      <textarea className="w-full pl-10 pr-4 py-3 rounded-lg border border-gray-300 focus:ring-2 focus:ring-blue-500 focus:outline-none" rows="3" placeholder="Jalan, No Rumah, Kecamatan, Kota..."></textarea>
                    </div>
                  </div>

                  <div>
                    <label className="block text-sm font-bold text-gray-700 mb-1">Metode Pembayaran</label>
                    <div className="grid grid-cols-2 gap-3">
                      <button type="button" className="border border-blue-500 bg-blue-50 text-blue-700 py-3 rounded-lg font-bold flex items-center justify-center gap-2">
                        <CreditCard size={18}/> Transfer Bank
                      </button>
                      <button type="button" className="border border-gray-200 text-gray-600 py-3 rounded-lg font-bold flex items-center justify-center gap-2 hover:bg-gray-50">
                        <Truck size={18}/> COD (Bayar di Tempat)
                      </button>
                    </div>
                  </div>
                </form>
              </div>
            </div>

            <div className="p-6 border-t border-gray-100 bg-gray-50">
              <button className="w-full bg-green-500 text-white py-4 rounded-xl font-bold text-lg hover:bg-green-600 transition shadow-lg flex justify-center items-center gap-2">
                 <MessageCircle size={20}/> Konfirmasi Pesanan via WA
              </button>
              <p className="text-center text-xs text-gray-500 mt-3">Data Anda aman. Kami akan mengarahkan ke WhatsApp Admin untuk detail pembayaran.</p>
            </div>
          </div>
        </div>
      )}

      {/* FLOATING WA BUTTON */}
      <a href="#" className="fixed bottom-6 right-6 bg-green-500 hover:bg-green-600 text-white p-4 rounded-full shadow-2xl z-50 transition transform hover:scale-110 flex items-center justify-center group">
        <span className="absolute right-full mr-4 bg-white text-gray-800 px-3 py-1 rounded-lg text-sm font-bold shadow-lg opacity-0 group-hover:opacity-100 transition whitespace-nowrap pointer-events-none">
          Chat CS Kami
        </span>
        <MessageCircle size={32} />
      </a>

    </div>
  );
};

export default App;